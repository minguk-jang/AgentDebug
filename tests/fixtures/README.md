# Test fixtures directory

This directory contains sample Langfuse traces used for testing.

## Fixtures

- `sample_trace_success.json`: A successful agent execution trace
- `sample_trace_failure.json`: A failed agent execution trace with errors
- `sample_trace_minimal.json`: Minimal trace structure for edge case testing
